package com.epam.hotel.command.factory;

/**
 * ENUMs of the all types of the commands.
 */
public enum CommandType {
    LOGIN,
    LOGOUT,
    USER_REGISTRATION,
    LOGOFF,
    SEND_INVOICE,
    ORDER_HANDLING,
    REQUEST,
    CANCEL_REQUEST,
    REQUEST_HANDLING,
    ADMIN_CABINET,
    CLIENT_CABINET,
    ACCOUNT,
    TOP_UP,
    PAY;
}
